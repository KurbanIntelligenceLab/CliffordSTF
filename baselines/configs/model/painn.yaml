# PaiNN baseline (SchNetPack-backed). Requires the [baselines] extra
# (pip install cliffordstf[baselines]) so that schnetpack is available.
model:
  name: painn
  hidden_channels: 144
  num_layers: 4
  num_rbf: 64
  cutoff: 6.0
  max_neighbors: 50
  num_elements: 100
  direct_forces: true
